# /run is tmpfs and is wiped on every boot. Recreate the runtime dir that
# holds the privileged-helper socket (#33) before wattpost-helper.socket
# activates. The socket itself is root:wattpost 0660 (set by the .socket
# unit); the dir only needs to be traversable.
d /run/wattpost 0755 root root -
